export enum GameMutation {
    SET_GAME_INSTALLATIONS = 'setGameInstallations',
    SET_CURRENT_GAME_PATH = 'setCurrentGamePath',
}

export enum StoreModule {
    // 全局模块，存放userinfo等全局信息
    GAME = 'GAME',
}